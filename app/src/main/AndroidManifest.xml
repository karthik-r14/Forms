<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.userdetails.forms">
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/form"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity
      android:name=".FormActivity"
      android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity
      android:name=".UserDetailShowActivity"
      android:label="@string/show_details"
      android:screenOrientation="portrait">
    </activity>
    <activity
      android:name=".view.about_us.AboutUsActivity"
      android:label="@string/about_us"
      android:screenOrientation="portrait">
    </activity>
    <activity android:name=".view.settings.SettingsActivity"
      android:label="@string/settings"
      android:screenOrientation="portrait">
    </activity>
  </application>

</manifest>